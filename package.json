{
  "name": "qrcode-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist --nojekyll",
    "deploy:docs": "npm run build && rm -rf docs && cp -r dist docs && touch docs/.nojekyll"
  },
  "dependencies": {
    "element-ui": "^2.15.14",
    "qrcodejs2-fixes": "^0.0.2",
    "vue": "^2.7.16",
    "vue-router": "^3.6.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^2.3.3",
    "gh-pages": "^6.3.0",
    "sass": "^1.77.8",
    "vite": "^4.5.9"
  }
}
